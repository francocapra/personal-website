{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "serve": "^14.2.4",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
